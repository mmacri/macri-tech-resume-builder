
import React from 'react';
import HomeContent from '@/components/home/HomeContent';

/**
 * Home page component serving as the main entry point for the resume website
 * Using static content, not database-driven
 */
const Home = () => {
  return <HomeContent />;
};

export default Home;
